/* eslint-disable filenames-simple/naming-convention -- Plugin doesn't work well with these types of filenames */

const rootConfig = require('./index');

/**
 * @type {import("eslint").Linter.Config}
 */
module.exports = rootConfig;
